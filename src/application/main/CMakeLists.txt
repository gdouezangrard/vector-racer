### SETUP APPLICATION ###
SET(TARGET_NAME main)
SET(TARGET_SRC main.c)
SET(TARGET_ADDED_LIBRARIES
	vectorMap
	vectorGame
	vectorSolver
	vectorSDL
	vectorDll
	SDL
)

### REGISTER ###
ADD_EXECUTABLE(${TARGET_NAME} ${TARGET_SRC})
TARGET_LINK_LIBRARIES(${TARGET_NAME} ${TARGET_ADDED_LIBRARIES})
